import RegisterController from "./RegisterController";

const Register: angular.IComponentOptions = {
  template: require('./Register.html'),
  controller: RegisterController
};

export default Register;
